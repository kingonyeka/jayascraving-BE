import { Repository, DataSource } from 'typeorm';
import { ConfigService } from '@nestjs/config';
import { Order } from './entities/order.entity';
import { OrderItem } from './entities/order-item.entity';
import { User } from '../users/entities/user.entity';
import { CartService } from '../cart/cart.service';
import { QueuesService } from '../queues/queues.service';
import { StaffService } from '../staff/staff.service';
import { PromotionsService } from '../promotions/promotions.service';
import { NotificationsService } from '../notifications/notifications.service';
import { AbandonedCartService } from '../abandoned-cart/abandoned-cart.service';
import { RealTimeAnalyticsService } from '../real-time-analytics/real-time-analytics.service';
import { InAppNotificationsService } from '../in-app-notifications/in-app-notifications.service';
import { PushNotificationsService } from '../push-notifications/push-notifications.service';
import { CreateOrderInput } from './dto/create-order.input';
import { UpdateOrderStatusInput } from './dto/update-order-status.input';
import { OrderFilterInput } from './dto/order-filter.input';
import { PaginationInput } from '../../common/types/pagination.type';
import { IPaginatedResult } from '../../common/types/paginated-result.type';
export declare class OrdersService {
    private readonly orderRepo;
    private readonly orderItemRepo;
    private readonly userRepo;
    private readonly cartService;
    private readonly queuesService;
    private readonly staffService;
    private readonly promotionsService;
    private readonly notificationsService;
    private readonly abandonedCartService;
    private readonly realTimeAnalyticsService;
    private readonly inAppNotificationsService;
    private readonly pushNotificationsService;
    private readonly configService;
    private readonly dataSource;
    constructor(orderRepo: Repository<Order>, orderItemRepo: Repository<OrderItem>, userRepo: Repository<User>, cartService: CartService, queuesService: QueuesService, staffService: StaffService, promotionsService: PromotionsService, notificationsService: NotificationsService, abandonedCartService: AbandonedCartService, realTimeAnalyticsService: RealTimeAnalyticsService, inAppNotificationsService: InAppNotificationsService, pushNotificationsService: PushNotificationsService, configService: ConfigService, dataSource: DataSource);
    private generateOrderNumber;
    createFromCart(userId: string, input: CreateOrderInput): Promise<Order>;
    findById(id: string): Promise<Order>;
    findByOrderNumber(orderNumber: string): Promise<Order>;
    findByUser(userId: string, pagination?: PaginationInput): Promise<IPaginatedResult<Order>>;
    findAll(filter?: OrderFilterInput, pagination?: PaginationInput): Promise<IPaginatedResult<Order>>;
    updateStatus(input: UpdateOrderStatusInput, adminId: string, adminName: string): Promise<Order>;
    cancel(orderId: string, userId: string): Promise<Order>;
    reorder(orderId: string, userId: string): Promise<Order>;
}
