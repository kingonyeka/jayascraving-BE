import { Repository, DataSource } from 'typeorm';
import { Product } from './entities/product.entity';
import { Category } from './entities/category.entity';
import { ProductVariant } from './entities/product-variant.entity';
import { ProductMedia } from './entities/product-media.entity';
import { CustomisationOption } from './entities/customisation-option.entity';
import { CreateProductInput } from './dto/create-product.input';
import { UpdateProductInput } from './dto/update-product.input';
import { ProductFilterInput } from './dto/product-filter.input';
import { PaginationInput } from '../../common/types/pagination.type';
import { IPaginatedResult } from '../../common/types/paginated-result.type';
import { StaffService } from '../staff/staff.service';
export declare class ProductsService {
    private readonly productRepo;
    private readonly categoryRepo;
    private readonly variantRepo;
    private readonly mediaRepo;
    private readonly optionRepo;
    private readonly staffService;
    private readonly dataSource;
    constructor(productRepo: Repository<Product>, categoryRepo: Repository<Category>, variantRepo: Repository<ProductVariant>, mediaRepo: Repository<ProductMedia>, optionRepo: Repository<CustomisationOption>, staffService: StaffService, dataSource: DataSource);
    private inferMediaType;
    private assertMediaArraysValid;
    getCategories(): Promise<Category[]>;
    createCategory(name: string, description?: string, imageUrl?: string): Promise<Category>;
    updateCategory(id: string, data: Partial<Category>): Promise<Category>;
    deleteCategory(id: string): Promise<boolean>;
    findAll(filter?: ProductFilterInput, pagination?: PaginationInput): Promise<IPaginatedResult<Product>>;
    findById(id: string): Promise<Product>;
    findBySlug(slug: string): Promise<Product>;
    getFeatured(): Promise<Product[]>;
    create(input: CreateProductInput, adminId: string, adminName: string): Promise<Product>;
    update(input: UpdateProductInput, adminId: string, adminName: string): Promise<Product>;
    delete(id: string, adminId: string, adminName: string): Promise<boolean>;
    toggleAvailability(id: string, adminId: string, adminName: string): Promise<Product>;
    toggleFeatured(id: string, adminId: string, adminName: string): Promise<Product>;
    addVariant(productId: string, name: string, type: string, additionalPrice: number): Promise<ProductVariant>;
    deleteVariant(variantId: string): Promise<boolean>;
    addCustomisationOption(productId: string, name: string, type: string, additionalPrice: number, isRequired: boolean, options?: string): Promise<CustomisationOption>;
    deleteCustomisationOption(optionId: string): Promise<boolean>;
}
